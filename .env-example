# Opcional: copie este archivo a .env para usar valores propios.
# Sin .env, el sistema usa credenciales de demostración y un JWT_SECRET aleatorio.

# Genere uno con: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=

USUARIO_LECTURA=lector
CLAVE_LECTURA=
USUARIO_ESCRITURA=operador
CLAVE_ESCRITURA=